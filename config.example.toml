[server]
host = "127.0.0.1"
port = 5317

# API access tokens; leave empty to disable authentication
# [[server.api_tokens]]
# token = "sk-your-token"
# description = "local dev"

# Email login example
[[accounts]]
email = "user1@example.com"
mobile = ""
area_code = ""
password = "pass1"

# China mobile login example
[[accounts]]
email = ""
mobile = "13800138000"
area_code = "+86"
password = "pass2"

# International mobile login example
[[accounts]]
email = ""
mobile = "1234567890"
area_code = "+1"
password = "pass3"


[account_provisioning]
# Optional temporary inbox provisioning. Disabled by default.
# This prepares disposable inboxes and stores provider credentials in the state file;
# DeepSeek signup automation is intentionally a separate integration step.
# enabled = false
# provider = "mail_tm"
# max_inboxes_per_run = 1
# password_length = 24
# poll_interval_ms = 5000
# poll_timeout_secs = 180
# state_path = "tmp/provisioned-inboxes.json"

[deepseek]
# Known-good User-Agent
# user_agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 Safari/537.36"

# API base URL (default)
# api_base = "https://chat.deepseek.com/api/v0"

# Full WASM asset URL (required for PoW; version changes — update manually)
# wasm_url = "https://fe-static.deepseek.com/chat/static/sha3_wasm_bg.7b9ca65ddd.wasm"

# X-Client-Version header (expert model features, etc.)
# client_version = "1.8.0"

# X-Client-Platform header
# client_platform = "web"

# Supported model types; default ["default", "expert"]
# Each type maps to an OpenAI model id: deepseek-<type>
# model_types = ["default", "expert"]
# max_input_tokens = [1048576, 1048576]
# max_output_tokens = [384000, 384000]
