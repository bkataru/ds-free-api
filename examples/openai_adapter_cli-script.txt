# openai_adapter_cli test script

# Check ds_core account pool status
status

# Basic non-streaming chat
chat examples/openai_adapter_cli/basic_chat.json

# Concurrent basic requests
concurrent 3 examples/openai_adapter_cli/basic_chat.json

# Basic streaming chat (JSON already has stream: true)
chat examples/openai_adapter_cli/stream_options.json

# Reasoning + search
chat examples/openai_adapter_cli/reasoning_search.json

# Tool call
chat examples/openai_adapter_cli/tool_call.json

# Stop sequence
chat examples/openai_adapter_cli/stop_sequence.json

# Raw mode: pass through raw output
chat examples/openai_adapter_cli/basic_chat.json --raw

# Model endpoints
models
model deepseek-default

quit
